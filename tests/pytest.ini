[pytest]
markers =
    slow: marks tests as slow (deselected by default, set E2E_RUN_SLOW=1)
    gcp: marks tests that need GCP credentials (deselected by default, set GOOGLE_APPLICATION_CREDENTIALS)
