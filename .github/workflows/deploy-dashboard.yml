name: CD - Deploy Dashboard

# DEPRECATED — remplacé par le module Terraform dashboard/ + ci.yml (WIF-only)
# Le dashboard Streamlit est déployé via Terraform, pas via ce workflow.
on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dashboard deployed via Terraform (terraform/modules/dashboard/). Use ci.yml for API."
