name: CD - Build & Deploy

# DEPRECATED — remplacé par ci.yml (WIF-only, Gitleaks → Semgrep → Trivy → Deploy → E2E)
# Conservé temporairement pour référence. Supprimer après validation du nouveau pipeline.
on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use ci.yml instead (WIF-based, no SA keys)"